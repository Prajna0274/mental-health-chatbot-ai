{
  "buildCommand": "cd nextjs-app && npm install && npm run build",
  "outputDirectory": "nextjs-app/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/nextjs-app/$1"
    }
  ]
}
